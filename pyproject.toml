[project]
name = "lettria-perseus-mcp"
version = "0.1.0"
description = "MCP server exposing Lettria's Perseus Text-to-Graph engine via the perseus-client SDK."
readme = "README.md"
license = { text = "MIT" }
authors = [
    { name = "jalakoo", email = "jalakoo@gmail.com" }
]
requires-python = ">=3.11"
keywords = ["mcp", "llm", "knowledge-graph", "text-to-graph", "perseus", "lettria"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "mcp[cli]>=2.0.0",
    "perseus-client>=1.0.0rc19",
    "python-dotenv>=1.0.0",
]

[project.urls]
Homepage = "https://github.com/Lettria/perseus-mcp"
Repository = "https://github.com/Lettria/perseus-mcp"
Documentation = "https://github.com/Lettria/perseus-mcp#readme"
Issues = "https://github.com/Lettria/perseus-mcp/issues"

[project.scripts]
lettria-perseus-mcp = "lettria_perseus_mcp:main"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
